receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  prometheus:
    endpoint: prometheus:9090
    namespace: default


service:
  pipelines:
    metrics:
      receivers: [otlp]
      exporters: [prometheus]